"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.obj2scheme = void 0;
let schemeFiller = (scheme, root) => {
    for (let key in root) {
        if (typeof root[key] === "object") {
            if (root[key].length) {
                scheme.properties[key] = { type: "array", length: root[key].length };
            }
            else {
                scheme.properties[key] = { type: "object", properties: {} };
                schemeFiller(scheme.properties[key], root[key]);
            }
        }
        else
            scheme.properties[key] = { type: typeof root[key] };
    }
};
let obj2scheme = (jsonObj) => {
    let scheme = { type: "object", properties: {} };
    schemeFiller(scheme, jsonObj);
    if (typeof jsonObj !== 'object')
        scheme = { type: "error", properties: { message: "please, pass a json object" } };
    return scheme;
};
exports.obj2scheme = obj2scheme;
//# sourceMappingURL=core.js.map